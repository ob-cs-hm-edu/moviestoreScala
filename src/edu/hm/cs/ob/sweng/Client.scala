package edu.hm.cs.ob.sweng

/* Die Client-Klasse.
 * In ihr fehlen noch:
 * 1. Ein getter für das Alter mit dem Namen age.
 * 2. Eine Redefinition von toString, die nur den Namen anzeigt.
 * Schauen Sie sich zum Vergleich die Klasse Movie an.
 */
class Client(name: String, ageVal: Int) {
  /* Die folgende Zeile macht gar keinen Sinn und muss gelöscht
   * werden. Sie ist hier nur enthalten, damit alles ohne Fehler compiliert.
   */
  val age = 100
}
